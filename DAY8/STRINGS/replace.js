const text = "The cat is cute. The cat is lazy.";
const result = text.replace("cat", "dog");
console.log(result);
//replace(searchValue, newValue)
//Replaces the first occurrence of searchValue with newValue.